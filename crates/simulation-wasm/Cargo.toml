[package]
name = "simulation-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1"
simulation-core = { path = "../simulation-core" }
simulation-protocol = { path = "../simulation-protocol" }
simulation-save = { path = "../simulation-save" }
simulation-worldgen = { path = "../simulation-worldgen" }
wasm-bindgen = "0.2"

[lints]
workspace = true
